To run app.py, create an environment with the required dependencies:

```conda env create -f environment.txt```

Then activate the environment:

```conda activate <env_name>```

Then run the app:

```python app.py```